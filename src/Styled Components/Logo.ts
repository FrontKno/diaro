import styled from "styled-components"

export const Logo = styled.h1`
           color: rgba(3, 0, 197, 0.6);
           font-family: 'Playfair Display', serif;
           font-size: 4em;
           margin: 0;
           text-align: center;
           transform: translate(-70px, -25px);
       `;